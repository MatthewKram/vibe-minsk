<script setup lang="ts">import { onMounted } from 'vue';import TopBar from '@/components/ui/TopBar.vue';import { useEventsStore } from '@/stores/events';import { formatDate } from '@/services/format';const s=useEventsStore();onMounted(()=>s.loadMy());</script>
<template><section class="screen"><TopBar back title="Мои события"/><header class="page-title"><h1>Мои события</h1><p>{{s.my.length}} создано</p></header><div class="stack"><RouterLink v-for="e in s.my" :key="e.id" :to="`/manage/${e.id}`" class="manage-row"><div><small>{{e.status}} · {{formatDate(e.startAt)}}</small><b>{{e.title}}</b><span>{{e.people}} / {{e.capacity}} участников</span></div><span>Управлять</span></RouterLink><div v-if="!s.my.length" class="empty">Событий пока нет.</div></div></section></template>
